name: Update Tensorix Models

on:
  schedule:
    # Run daily at 00:00 UTC
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-models:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch and update models
        run: node scripts/update-models.js
        env:
          TENSORIX_API_KEY: ${{ secrets.TENSORIX_API_KEY }}

      - name: Check for changes
        id: check-changes
        run: |
          if git diff --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request
        if: steps.check-changes.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: 'feat: update tensorix models from API'
          title: 'feat: update tensorix models from API'
          body: |
            Automated update of Tensorix models from https://api.tensorix.ai/v1/models

            Changes include:
            - Updated models in `models.json`
            - Updated model table in `README.md`
          branch: update-tensorix-models
          delete-branch: true
